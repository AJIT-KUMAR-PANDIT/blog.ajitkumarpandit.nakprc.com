fragment Post on Post {
	id
	title
	url
	author {
		name
		profilePicture
	}
	coverImage {
		url
	}
	publishedAt
	slug
	brief
	tags {
		name
		slug
	}
	readTimeInMinutes
	comments(first: 0) {
		totalDocuments
	}
}
